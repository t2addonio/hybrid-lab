# Copy to .env locally. Never commit real keys.

# Optional hybrid path for preferred roles (residual_stream, researcher, rf, quantum_sim, coordinator)
XAI_API_KEY=

# Optional additional edge_bridge frontends
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
